#include <iostream>

int main()
{

    // int x = 42;
    // int y = 123;

    // int * p = &x;
    // int * q = &y;
    // std::cout << *p << '\n';

    // p = q;

    // std::cout << *p << '\n';

    // int x = 42;
    // int y = 43;
    // int * p = &x;

    // std::cout << &x << ' ' << &y << ' ' << p << '\n';
    // p = &y;

    // std::cout << &x << ' ' << &y << ' ' << p << '\n';

    // int x = 42;
    // int y = 123;
    // int * p = &x;
    // int * q = &y;

    // std::cout << &x << ' ' << &y << ' ' << p  << ' ' << q << '\n';

    // p = &y;
    // std::cout << &x << ' ' << &y << ' ' << p  << ' ' << q << '\n';

    // q = &x;
    // std::cout << &x << ' ' << &y << ' ' << p << ' ' << q << '\n';

    // int i = 42;
    // int j = 5;
    // int k = -2;
    // int * p = &i;
    // int * q = &j;
    // int * r = p;
    // p = q;
    // q = r;
    // std::cout << *p << ' ' << *q << ' ' << *r << '\n';

    // int x = 0;
    // int y = 1;
    // int * p = &x;
    // int * q = &y;
    // std::cout << (p == q) << '\n'; // boolean expression // returns false
    
    
    return 0;
}
